import React from 'react'

const UserManager = () => {

  const initState = {count : 0}

  const reducer = (state, action) => {
    switch () {
      ca
    }
  }

  return (
    <div>UserManager</div>
  )
}

export default UserManager
